{
  "name": "restaurant-ms",
  "version": "1.0.0",
  "description": "Restaurant Management System — Full Stack",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "cd backend && npm run dev",
    "install-backend": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
